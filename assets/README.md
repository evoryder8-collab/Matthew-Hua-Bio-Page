# Image assets

## Mindset Attention Field — final reveal image

The Mindset element (Six Elements section) ends on an editorial reveal that uses a
transparent figure of Matthew and Tony Robbins.

**Place the file here, with this exact name:**

```
assets/v2-mat-x-tony.webp
```

Notes:
- Use the transparent WebP you provided (originally named
  `v2 mat x tonyPHOTO-2026-03-16-21-22-09-2.webp`). Rename it to `v2-mat-x-tony.webp`
  (no spaces) so the URL is clean and reliable on GitHub Pages.
- Keep the transparent background. The layout already adds a soft glow, drop shadow and
  a bottom fade, so the figure reads as integrated, not pasted on.
- If you would rather keep a different filename, update the `ATTN_IMG` constant in
  `index.html` (search for `ATTN_IMG=`).
- Until the file is added, the final reveal still works: the image simply hides itself
  and a soft accent glow stands in its place, so nothing looks broken.
